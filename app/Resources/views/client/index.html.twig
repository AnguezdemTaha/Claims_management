{% extends 'base.html.twig' %}

{% block body %}
<link rel="stylesheet" href="{{asset("css/bootstrap.min.css")}}">
<link rel="stylesheet" href="{{asset("css/style1234.css")}}">
<div id="container">
<center><h1>Liste des clients</h1></center 
    <div class="row">
    <div class="col-md-10 col-lg-push-1" > 
    
    <ul>
        <li >
            <center>
            <a href="{{ path('client_new') }}"><input type="button" class="btn btn-warning" value="Ajouter un neuveux client"></a>
            </center>
            
        </li>
    </ul>
    
    </div>
    </div>
    <div id="row">
    <div class="col-md-10 col-lg-push-1" >
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th>Adress</th>
                <th>Date de naissance</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for client in clients %}
            <tr>
                <td><a href="{{ path('client_show', { 'id': client.id }) }}">{{ client.id }}</a></td>
                <td>{{ client.nom }}</td>
                <td>{{ client.prenom }}</td>
                <td>{{ client.email }}</td>
                <td>{{ client.adress }}</td>
                <td>{% if client.dateNaissance %}{{ client.dateNaissance|date('Y-m-d') }}{% endif %}</td>
                <td>
                    <ul >
                        {# <li class="list-group-item">
                            <a href="{{ path('client_show', { 'id': client.id }) }}"><p class="text-info">show</p></a>
                        </li>
                        <li class="list-group-item">
                            <a href="{{ path('client_edit', { 'id': client.id }) }}"<p class="text-warning">edit</p></a>
                        </li> #}
                        <li id="horiz">
                            <a href="{{ path('client_show', { 'id': client.id }) }}"><p class="text-info"><input type="button" class="btn btn-info" value="Show"></p></a>
                        </li>
                        <li id="horiz">
                            <a href="{{ path('client_edit', { 'id': client.id }) }}"<p class="text-warning"><input type="button" class="btn btn-warning" value="Edit"></p></a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
    </div>
    </div>
</div>
{% endblock %}
