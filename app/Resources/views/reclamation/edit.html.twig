{% extends 'base.html.twig' %}

{% block body %}
    <link rel="stylesheet" href="{{asset("css/bootstrap.min.css")}}">
    <link rel="stylesheet" href="{{asset("css/style1234.css")}}">
<div id="container">
<center><h1>Reclamation edit</h1></center>
    <div class="col-md-4 col-lg-push-4" >
    {{ form_start(edit_form) }}
        {# {{ form_widget(edit_form) }} #}

        <div class="form-group">{{form_row(edit_form.client,{'attr':{'class':'form-control'}})}}</div>

        <div class="form-group">
            {{form_label(edit_form.code)}}
            {{form_widget(edit_form.code,{'attr':{'class':'form-control'}})}}
        </div>

        <div class="form-group">
            {{form_label(edit_form.objet)}}
            {{form_widget(edit_form.objet,{'attr':{'class':'form-control'}})}}
        </div>

        <div class="form-group">
            {{form_label(edit_form.contenu)}}
            {{form_widget(edit_form.contenu,{'attr':{'class':'form-control'}})}}
        </div>
        <div id="buttons">
        <ul>
        <li id="horiz">
            <a href="{{ path('reclamation_index') }}"><input type="button" class="btn btn-warning" value="Retourner"></a>
        </li>
        <li id="horiz"><input class="btn btn-success" type="submit" value="Modifier" /></li>


        
    {{ form_end(edit_form) }}
    <li id="horiz">
            {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
        </ul>
    </div>
    </div>
    </div>
{% endblock %}
